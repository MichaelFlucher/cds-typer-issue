{
  "name": "cds-typer-bug",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^8",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/cds-typer": "^0.25.0",
    "@cap-js/cds-types": "^0.6",
    "@cap-js/sqlite": "^1"
  },
  "scripts": {
    "start": "cds-serve",
    "generate:types": "cds-typer srv --outputDirectory @cds-models --logLevel DEBUG"
  }
}
